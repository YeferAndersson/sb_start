import Container from '@/components/shared/Container'

const SubDirectorService = () => {
    return (
        <Container className="h-full">
            <h3 className='text-white'>Sub Director de Investigación, el que hace el sorteo de jurados, y panel similar al del coordinador</h3>
        </Container>
    )
}

export default SubDirectorService
